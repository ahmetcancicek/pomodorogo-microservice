spring:
  application:
    name: eureka-server
  cloud:
    config:
      profile: native
      uri: http://localhost:8761

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false